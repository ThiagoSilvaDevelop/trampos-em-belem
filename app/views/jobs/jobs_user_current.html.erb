<%= render 'home/nav_bar' %><br><br><br><br>

<div class="alert alert-success" role="alert">
  <h4 class="alert-heading">Aqui está listada todas as vagas que você publicou.</h4>
  <hr>
  <p class="mb-0">Para ficar organizado, pedimos que você adicione as informações das vagas com bastante detalhe, assim você terá mais resultados positivos dos candidatos
  e também ajudará a plataforma manter as informações mais padronizadas.</p>
</div>

<div class="container">
  <div class="row">
    <table class="table" id="jobs_user_current">
      <thead class="thead-dark">
        <tr>
          <th scope="col">#</th>
          <th scope="col">Categoria</th>
          <th scope="col">Data de Criação</th>
          <th scope="col">Vaga Preenchida?</th>
          <th scope="col">Ações</th>
        </tr>
      </thead>
      <tbody>
        <% @jobs.each_with_index do |job, count| %>
          <tr>
            <th scope="row"><%= count + 1 %></th>
            <td><%= link_to job_path(job.id), class: 'noHover' do %> <%= job.category %> <% end %></td>
            <td><%= job.created_at.strftime("%d-%m-%y") %></td>
            <td><i class="fa fa-times-circle" style="margin-left: 50px;"></i></td>

            <td>
              <%= link_to edit_job_path(job), type: "button", class: "btn btn-warning" do %>
                <i class="fa fa-pencil-square-o" aria-hidden="true"></i>
                Editar
              <% end %>

              <%= link_to job_path(job), method: :delete, type: "button", class: "btn btn-danger", :onclick => "alert('Deseja excluir essa vaga?')" do %>
                <i class="fa fa-trash-o" aria-hidden="true"></i>
                Exluir
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>

<style>
.noHover{
  color: #000;
}
.noHover:hover{
    color: #000;
    text-decoration: none;
}
</style>
